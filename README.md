# How to run

Create a virtual environment with:

```
python -m venv venv
```
Open your virtual environment:

```
source venv\bin\activate
```

Install flask:

```
pip install flask
```

Run script:

```
python server.py
```
